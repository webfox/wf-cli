module.exports = (platform, theme) => {
    // Need to add a check in here for platform and theme
    // If platform and theme are not availble ask user
    console.log(`Provisioning: ${platform} | With ${theme}`);
};
